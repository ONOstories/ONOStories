// This file is now replaced by the AuthContext
// Keeping it for backward compatibility, but it just re-exports from the context

export { useAuth } from '../contexts/AuthContext';
export type { UserProfile } from '../lib/mockData';